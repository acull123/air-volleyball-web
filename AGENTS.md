# Project Rules

- Any button or clickable control that uses the blue gradient hover background must force white text on hover, for example with `hover:!text-white` or `group-hover:text-white` on all visible text inside the control.
